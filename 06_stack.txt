DSA INTERVIEW NOTES — DAY 6
TOPIC: STACK

---

1. STACK PATTERN

---

Stack follows LIFO (Last In First Out).

Operations:

push()
pop()
top()

Used when:

* Next greater/smaller element
* Balanced parentheses
* Monotonic behavior
* Processing elements from right

---

2. VALID PARENTHESES

---

Problem:
Check if parentheses string is valid.

Rules:

* Every opening bracket has a matching closing bracket
* Order must be correct

---

## APPROACH

1. Push opening brackets into stack

2. For closing brackets:

   * Check if stack is empty → invalid
   * Check if top matches → pop
   * Otherwise → invalid

3. At end:

   * If stack empty → valid

---

## C++ CODE

bool isValid(string s){

```
stack<char> st;

for(char ch : s){

    if(ch=='(' || ch=='{' || ch=='[')
        st.push(ch);

    else{

        if(st.empty())
            return false;

        if((ch==')' && st.top()!='(') ||
           (ch=='}' && st.top()!='{') ||
           (ch==']' && st.top()!='['))
            return false;

        st.pop();
    }
}

return st.empty();
```

}

Time Complexity: O(n)
Space Complexity: O(n)

---

3. NEXT GREATER ELEMENT

---

Problem:
Find next greater element on right side.

---

## APPROACH (MONOTONIC STACK)

Traverse from right → left.

Steps:

1. Remove smaller elements from stack
2. Top gives next greater
3. If empty → -1
4. Push current element

---

## C++ CODE

vector<int> nextGreaterElement(vector<int>& nums){

```
int n = nums.size();
vector<int> ans(n);
stack<int> st;

for(int i=n-1;i>=0;i--){

    while(!st.empty() && st.top() <= nums[i])
        st.pop();

    if(st.empty())
        ans[i] = -1;
    else
        ans[i] = st.top();

    st.push(nums[i]);
}

return ans;
```

}

Time Complexity: O(n)
Space Complexity: O(n)

---

4. DAILY TEMPERATURES

---

Problem:
Find how many days until a warmer temperature.

---

## APPROACH

Traverse from right → left.

Stack stores indices.

Steps:

1. Remove smaller or equal temperatures
2. If stack empty → 0
3. Else → distance = st.top() - i
4. Push current index

---

## C++ CODE

vector<int> dailyTemperatures(vector<int>& temp){

```
int n = temp.size();
vector<int> ans(n);
stack<int> st;

for(int i=n-1;i>=0;i--){

    while(!st.empty() && temp[i] >= temp[st.top()])
        st.pop();

    if(st.empty())
        ans[i] = 0;
    else
        ans[i] = st.top() - i;

    st.push(i);
}

return ans;
```

}

Time Complexity: O(n)
Space Complexity: O(n)

---

## DAY 6 SUMMARY

Pattern Learned:
Stack

Problems Solved:

1. Valid Parentheses
2. Next Greater Element
3. Daily Temperatures

Key Concepts:

* Monotonic Stack
* Right-to-left traversal
* Stack stores useful candidates
* Each element pushed/popped once

---

## END OF DAY 6
