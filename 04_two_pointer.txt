DSA INTERVIEW NOTES — DAY 4
TOPIC: TWO POINTERS

---

1. TWO POINTER PATTERN

---

Used when:

* array is sorted
* pair or triplet problems
* minimize or maximize distance
* remove duplicates
* container / area problems

General idea:

left pointer  -> start
right pointer -> end

Move pointers depending on conditions.

---

2. TWO SUM II (SORTED ARRAY)

---

Problem:
Given a sorted array, find two numbers such that:

numbers[i] + numbers[j] = target

Return their indices (1-indexed).

Example:

numbers = [2,7,11,15]
target = 9

Output:
[1,2]

---

## OPTIMAL APPROACH

Use two pointers.

left = 0
right = n-1

If sum is too small -> move left pointer
If sum is too large -> move right pointer

---

## C++ CODE

vector<int> twoSum(vector<int>& numbers, int target){

```
int left = 0;
int right = numbers.size() - 1;

while(left < right){

    int sum = numbers[left] + numbers[right];

    if(sum == target)
        return {left+1, right+1};

    else if(sum < target)
        left++;

    else
        right--;
}

return {};
```

}

Time Complexity: O(n)
Space Complexity: O(1)

---

3. CONTAINER WITH MOST WATER

---

Problem:
Given heights of vertical lines, find two lines
that together form a container holding maximum water.

Area formula:

area = min(height[left], height[right]) * (right-left)

Example:

height = [1,8,6,2,5,4,8,3,7]

Output:
49

---

## KEY IDEA

Area is limited by the smaller height.

Therefore move the pointer with smaller height.

---

## C++ CODE

int maxArea(vector<int>& height){

```
int left = 0;
int right = height.size()-1;

int maxArea = 0;

while(left < right){

    int area = min(height[left], height[right]) * (right-left);

    maxArea = max(maxArea, area);

    if(height[left] < height[right])
        left++;
    else
        right--;
}

return maxArea;
```

}

Time Complexity: O(n)
Space Complexity: O(1)

---

4. 3SUM

---

Problem:
Find all unique triplets such that:

nums[i] + nums[j] + nums[k] = 0

Example:

nums = [-1,0,1,2,-1,-4]

Output:

[-1,-1,2]
[-1,0,1]

---

## KEY IDEA

1. Sort the array.
2. Fix one element.
3. Use two pointers to find remaining two numbers.

Also skip duplicates to avoid repeating triplets.

---

## C++ CODE

vector<vector<int>> threeSum(vector<int>& nums){

```
vector<vector<int>> ans;

sort(nums.begin(), nums.end());

for(int i=0;i<nums.size();i++){

    if(i>0 && nums[i]==nums[i-1])
        continue;

    int left = i+1;
    int right = nums.size()-1;

    while(left < right){

        int sum = nums[i] + nums[left] + nums[right];

        if(sum == 0){

            ans.push_back({nums[i], nums[left], nums[right]});

            while(left<right && nums[left]==nums[left+1])
                left++;

            while(left<right && nums[right]==nums[right-1])
                right--;

            left++;
            right--;
        }

        else if(sum < 0)
            left++;

        else
            right--;
    }
}

return ans;
```

}

Time Complexity: O(n²)
Space Complexity: O(1) (excluding output)

---

## DAY 4 SUMMARY

Pattern Learned:
Two Pointers

Problems Solved:

1. Two Sum II
2. Container With Most Water
3. 3Sum

Key Concepts:

* Move pointers based on sorted order
* Smaller element limits container area
* Skip duplicates in triplet problems
* Two pointers reduce nested loops

---

## END OF DAY 4
